# 2026-06-02 — Batch N semantic-near 广度扩展

## 完成工作

### A. 模块分类扩展 (builder.py)
- 新增 `ipv6.prefix_list` 分类
- 新增 BGP 子模块：`bgp.peer_group`, `bgp.activation`
- 新增 L2 安全子模块：`l2.bpduguard`, `l2.hybrid`
- 新增路由子模块：`ospfv3.interface`
- 将 `zone` 纳入 `_MANUAL_REVIEW_FEATURES`
- 接口子特征提取：`_l2_security_specs_from_interface` (lldp/cdp, storm-control, port-security, bpduguard, hybrid, dhcp-snooping)
- 接口路由子特征提取：`_routing_interface_specs_from_interface` (ospfv3, isis)

### B. semantic_near suggested_lines 扩展 (translator.py)
- interface range：trunk allowed/native vlan、storm-control、port-security、bpduguard
- management.line：acl/access-class、privilege、idle-timeout
- radius/tacacs scheme：目标厂商骨架 + `<redacted>`
- route-policy：community、as-path、next-hop match/set
- route_filter：as-path-filter、community-filter
- ipv6：prefix_list、ospfv3.interface
- firewall：zone interface binding
- snmp：group/user/trap
- syslog：source-interface
- ntp：vrf/source-interface

### C. fallback 保守修复
- `router_rules.py`：修复 cisco→h3c 的 OSPF/BGP 头翻译（`router ospf`→`ospf`、`router bgp`→`bgp`）
- `evaluate_corpus_fallback.py`：residue 检查改为行级 prefix 匹配，消除 `security-zone name` 对 `zone name` 的误报

### D. 新增测试
- `tests/test_module_graph_batch_n_expansion.py`：30 个测试，覆盖 interface/management/access/routing/IPv6/firewall/security isolation

### E. 文档同步
- 更新 BETA_ACCEPTANCE、BETA_READINESS_REPORT、RUNBOOK、AGENTS.md 中测试计数：2345→2376
- 更新 FALLBACK_GAP_ANALYSIS：GAP-RT-01、GAP-FW-03 标记为已解决，24/24 通过
- 更新 beta_acceptance JSON 和 test_beta_acceptance_docs.py

## 验证结果
- Full pytest：2376 passed, 29 skipped, 0 failed
- CI quality gates：2376 passed, 0 regressions
- Corpus fallback evaluator：24/24 passed (100.0%), 0 active gaps
- git status：clean
- commit：20c8738

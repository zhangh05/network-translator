# Python runtime artifacts
__pycache__/
*.py[cod]
.pytest_cache/

# Local virtual environments
venv/
.venv/
.venv-local/
env/

# Service runtime
.run/
logs/

# Local/runtime state
memory_data/
projects/
llmsetting.json
bench/harness_results.json
bench/*.tmp
cache_data/
.semantic_cache/
knowledge/.semantic_cache/
knowledge_data/.semantic_cache/

# Corpus — raw samples never committed
corpus/samples/

# Live report caches (regeneratable)
docs/coverage/live_corpus_cache.json
docs/coverage/live_corpus_pilot*.json
docs/coverage/live_corpus_pilot*.md
bench/live_report_p0.json
reports/coverage_matrix.json

# Tool and editor noise
.claude/settings.local.json
.DS_Store
Thumbs.db

# Agent workspace (local memory, not committed)
.workbuddy/

